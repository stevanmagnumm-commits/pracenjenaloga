// Auto-refresh is disabled — all refreshes are manual-only.
// Scheduler kept as a no-op to avoid breaking any imports.

export function startScheduler() {
  // intentionally empty
}

export function stopScheduler() {
  // intentionally empty
}
